services:
  app:
    build: .
    ports:
      - "8080:8080"
    depends_on:
      - db

  db:
    image: oscarfonts/h2:latest
    ports:
      - "8082:8082"
    environment:
      H2_OPTIONS: "-tcp -tcpAllowOthers -ifNotExists"